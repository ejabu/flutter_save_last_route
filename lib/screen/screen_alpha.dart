import 'package:flutter/material.dart';

class ScreenAlpha extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Screen Alpha"),
      ),
      body: Center(
        child: Text("Screen Alpha"),
      ),
    );
  }
}