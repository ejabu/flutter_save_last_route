import 'package:flutter/material.dart';

class ScreenDelta extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Screen Delta"),
      ),
      body: Center(
        child: Text("Screen Delta"),
      ),
    );
  }
}