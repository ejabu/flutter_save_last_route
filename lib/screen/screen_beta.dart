import 'package:flutter/material.dart';

class ScreenBeta extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Screen Beta"),
      ),
      body: Center(
        child: Text("Screen Beta"),
      ),
    );
  }
}